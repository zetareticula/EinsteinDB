[package]
name = "security"
version = "0.1.0"
edition = "2018"
publish = false

authors = ["WHTCORPS INC"]
license = "Apache-2.0"

[lib]
name = "security"
path = "src/lib.rs"

[dependencies]
log = "0.4"
serde = { version = "1.0", features = ["derive"] }
