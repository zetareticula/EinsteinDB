[package]
name = "txn_types"
version = "0.1.0"
edition = "2018"
publish = false

authors = ["WHTCORPS INC"]
license = "Apache-2.0"

[lib]
name = "txn_types"
path = "src/lib.rs"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
