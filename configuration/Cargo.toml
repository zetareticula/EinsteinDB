[package]
name = "configuration"
version = "0.1.0"
edition = "2018"
publish = false

authors = ["WHTCORPS INC"]
license = "Apache-2.0"

[lib]
name = "configuration"
path = "src/lib.rs"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
toml = "0.5"
